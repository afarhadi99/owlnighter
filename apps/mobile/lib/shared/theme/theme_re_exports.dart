/// Convenience re-export so feature files import theme/motion from one place
/// inside the app rather than reaching into the design_system package path
/// repeatedly. Keeps feature imports short and swappable.
export 'package:design_system/design_system.dart';
